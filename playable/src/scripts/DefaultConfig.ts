import { Background } from "./Background";
import { Main } from "./Main";

export const defaultConfig = {
    bg: { class: Background, id: 'bg', texture: 'bg' },             // Background
    main: { class: Main }                                           // Main container
}